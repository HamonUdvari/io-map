---
import "../styles/global.css";
import MapCanvas from "../components/MapCanvas";
import YearTimeline from "../components/YearTimeline";
import CategoryFilters from "../components/CategoryFilters";
import Organisations from "../components/Organisations";
import Button from "../components/Button";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>io-map</title>
  </head>
  <body class="grid grid-cols-1 grid-rows-1">
    <MapCanvas
      client:only="preact"
      class="col-start-1 col-end-1 row-start-1 row-end-1"
    />

    <div
      class:list={[
        "col-start-1 col-end-1 row-start-1 row-end-1 z-10",
        "pointer-events-none", // the overlay must not eat map gestures; .nav re-enables
        "grid h-dvh grid-rows-[auto_1fr]", // nav row + the drawer's (expanded) row; definite height, or 1fr grows to the drawer's content
        "overflow-clip", // the collapsed drawer hangs below the viewport — no page scroll
      ]}
    >
      <nav class="nav">
        <h1 class="nav-title inline-block">
          <Button href="#" class="">
            {" "}
            Mapping International Geneva{" "}
          </Button>
        </h1>

        <Button href="#" class="nav-lang text-base">
          FR
        </Button>

        <YearTimeline client:only="preact" class="nav-timeline" />
        <CategoryFilters client:only="preact" class="nav-filters" />
      </nav>

      <Organisations client:only="preact" />
    </div>
  </body>
</html>
